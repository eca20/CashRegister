# Supply these through the shell/container; this project does not auto-load .env.
HOST=127.0.0.1
PORT=3000
# false exposes only the calculation API and health endpoints.
SERVE_WEB=true
REQUEST_TIMEOUT_MS=10000
SHUTDOWN_TIMEOUT_MS=15000
# No database URL or remote API credentials are consumed by the default service.
